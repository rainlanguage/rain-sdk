import { expect } from 'chai';
import { CRP } from '../src/crp';
// @ts-ignore
import { ethers } from "hardhat";

describe('Configurable Rights Pool', () => {

  it('returns fromAddress', async () => {

    // const signers = await ethers.getSigners();
    // const creator = signers[0];

    // let result = await CRP.fromAddress('1', creator , 1);
    // expect(result).to.be.equal('');
  });

});
